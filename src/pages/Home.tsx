import { Typography, Container } from "@mui/material";

const Home = () => (
  <Container sx={{ mt: 8 }}>
    <Typography>
      ようこそ！。上のボタンから使ってください！
    </Typography>
  </Container>
);

export default Home;
